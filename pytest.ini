[pytest]
testpaths = tests
markers =
    integration: 需要真实浏览器 + WebDriver 的端到端测试（缺环境时会自动 skip）

# 离线/CI 跑法（用 marker 而不是 --ignore，模块自身也会 skip）：
#   pytest tests/ -m "not integration"
# 只跑 E2E：
#   pytest tests/ -m integration
