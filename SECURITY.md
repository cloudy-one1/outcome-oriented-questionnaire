# 安全策略

## 报告一个安全问题

用 GitHub 的**私有漏洞报告**（仓库首页 → Security → Report a vulnerability）。
不要开公开 issue —— 缺陷细节在这个仓库里等于"怎样读到别人的答卷"。

公开 issue 里也请**不要贴**：

- 能直接定位到某一份真实问卷的完整 URL（微信带进来的渠道参数里含投放者信息）
- 运行日志或 CSV 导出的原文（填空题的姓名、手机、邮箱就在里面）

复现需要这些信息时，把它们换成占位符，或走私有报告。

## 这个工具会碰到什么数据

它操作的是浏览器里的真实问卷页，所以落盘的东西天然带个人信息：

| 位置 | 内容 | 是否入库 |
|---|---|---|
| `data/history.db` | 批次元信息 + 逐题答案明细，`answers.text_answer` 是填空题**原文** | 已在 `.gitignore` 里，绝不提交 |
| `configs/*.json` | 你在权重表里手写的候选文本池（可能含真实姓名/地址池） | 已在 `.gitignore` 里 |
| CSV 导出、运行日志 | 同上 | 已在 `.gitignore` 里 |

Python 侧不向任何第三方发请求：`src/` 与 `webui/` 里没有 `requests` / `urllib` 调用，
唯一的网络出口是浏览器按你给的那个问卷地址打开问卷星页面。注入页面的 JS 全部由
`src/interactions/_scripts.py` 在本仓库内生成，不引外部脚本。

## 本地控制面（Web 控制台）

`wjx-web` / `python run_web.py` 起的是一个 stdlib `ThreadingHTTPServer`，它是这套工具的
**控制面**：能开批次、能停、能改权重配置、能读运行历史。现状边界是四条，都不是"待修"：

- **绑定写死在回环**：`webui/server.py` 的 `host` 默认 `127.0.0.1`，命令行只有
  `--port` 与 `--open-browser`，**没有 `--host` 开关** —— 想监听别的地址要改代码。
- 浏览器来的请求要过两道：`Host` 必须是回环名，且 `Origin` 存在时必须与之完全同源
  （`webui/api.py`，后者挡 DNS rebinding —— 攻击者把自己域名解析到 `127.0.0.1` 之后，
  浏览器发出的 `Host` 就成了那个域名）。
- **没有鉴权**：Origin 缺席时第二道直接放行，所以本机任意进程都能驱动这个控制面。
  端口一旦被挪出回环（端口转发、局域网共享、反向代理、容器端口映射），任何能连到它的
  人不需要凭据就能开批次、改配置、导出含个人信息的 CSV。
- 破坏性动作有第二次确认：清空运行历史要先把 `purge_preview()` 的范围认一遍，
  再带一次性凭据调 `purge_confirm` —— 一个请求删不掉历史。

据此：**把控制面挪出回环属于配置风险，不是实现漏洞**。若哪天真要把它做成可远程访问的
产品能力，鉴权、CSRF 与"谁能驱动谁"必须先一起做，那时缺它们才算缺陷。

## 什么算安全问题

- 上面三类数据经由任何路径发到问卷域之外
- 回环绑定或 `Host` / `Origin` 两道判据被绕过（同源页面之外的浏览器能驱动控制面、
  非回环地址被接受）
- 只带一个请求就能删掉历史或改配置（`purge_confirm` 那次确认被绕过）
- 页面文案（题干、选项、提示）能让工具做出用户没要求的行为 —— 例如改提交目标地址、
  把答案带进另一个请求
- 提交前完整度自检、协议框诊断这类**拦停判据**能被页面内容绕过
- 钉住的依赖被劫持或需要替换来源

## 什么不算安全问题

- 被平台风控、提交被拒、账号受限：那是平台使用条款问题，见
  [README 的免责声明](README.md#免责声明)，请走普通 issue
- 需要已获得本机用户权限才能读到的 `data/history.db`：本地磁盘访问按已沦陷处理
- 移动端投放问卷探测不到题目：那是未适配，不是绕过

## 支持版本

只有 `main` 与最新发布版本修安全问题。`1.x` / `2.x` 不再单独出补丁 —— 需要修复请升到当前版本。

## 未开启的能力

本仓库不提供安全更新承诺（没有 SLA）、不设漏洞悬赏。它是个人学习项目，
维护者按上面那几条处理，回复可能慢。
