# 信度系数（Cronbach α）方案 —— 设计稿，不含实现

> 状态：**P0 与 P1 已落地（v3.2，2026-09-23），P2 只做完一半**。
> P0 = `src/reliability.py` + CLI `--report-alpha`（只测不改）；P1 = `src/plan.py`
> 的计划矩阵 / 按秩映射 / 一维 σ_e 搜索 + CLI `--alpha-target`（**仅 CLI**，GUI 没有
> 滑条，§0 那条硬边界因此还没被推到人手上）。两处与本稿的偏离要说清：
> ① §4 要求的 schema 3.0 → 3.1 没做，`dimension` / `reverse` 是直接穿过
> `config_io` 的校验活下来的（校验器对未知字段宽容），没有版本位也就没有兼容读入的口径；
> ② §3 设想实测 α 只算 `success`，而历史库**没有逐份成败标记**（`answers` 早于点提交
> 落盘），所以 P2 的"实测 / 含 unknown 区间"两栏合成了一栏加一句 `scope_caveat`。
> 这份稿子原本的用途（一次决定要不要做、做到哪一步）已经用完，留着当作实现口径的对照。对标来源：SurveyController
> `software/core/psychometrics/`（`psychometric.py:38-57`、`joint_optimizer.py`），
> **只借数学与分层，不取代码**（它 GPL-3.0，本仓库 MIT）。

---

## 0. 先说清这件事的性质（决定了它的边界）

本项目已经在生成 synthetic 答卷。让同一份答卷内部**更自洽**（B1 persona 已经做了人口学
那一半），与让**量表维度之间的相关结构**达到某个 α，是同一条路的两段。前者是"别长出
一个不存在的人"，后者是"让造出来的数据通过信度检验"。

因此本方案自带一条硬边界：**只用于自有或已获授权的测试问卷上验证算法**。README 的
免责声明（禁刷票刷量）在这个功能上不是套话 —— α 目标一旦进 GUI 滑条，工具的能力边界
就从"填得完"扩到"填得像真研究数据"。若做 P1，README 免责声明一节必须同步点名这一条。

## 1. 目标 / 非目标

**目标**
- G1：能**测**——给定一个批次的已提交答案，报出各维度的 Cronbach α。
- G2：能**控制**——给定目标 α（夹在 0.60~0.95）与每道题的选项边际分布，生成一批
  答卷，使其**实际投递成功**的数据的 α 落在目标附近。
- G3：能**自证**——计划 α 与实测 α 的差要摆在批次结束的统计里，不许只报计划。

**非目标**
- 不做多维因子结构（单维 α 只对应一个维度一组题）。
- 不为"提高通过率"服务：α 控制不改变提交语义、不碰三态判定。
- 不支持多选 / 排序 / 填空参与 α（它们没有自然的等距计分，硬套是伪科学）。

## 2. 数学（一步不藏）

对 k 个题（item）、n 份答卷，Cronbach α：

```
α = k/(k-1) · (1 − Σᵢ Var(Xᵢ) / Var(S))
```

平行测量模型下 `Xᵢ = θ + eᵢ`（θ 为该样本的潜变量，eᵢ 独立同分布），题间平均互相关

```
ρ = α / (k − α(k−1))          ⟺   α = kρ / (1 + ρ(k−1))
```

误差方差由 ρ 反解（归一取 `Var(θ) = 1`，此时 `Cov(Xᵢ,Xⱼ) = 1`、`Var(Xᵢ) = 1 + σ_e²`、
`ρ = 1/(1 + σ_e²)`）：

```
σ_e = sqrt(1/ρ − 1)
```

**生成过程**：对每份答卷抽 `θ ~ N(0,1)`，对每道参与题算得分
`X = ±θ + σ_e·ε`（ε~N(0,1)，`±` 由该题是否反向决定），然后把这一列得分
**按秩映射**到该题的选项配额向量上（排名落在哪一段就选哪个选项）。

关键性质：秩映射对得分的任意单调变换不变，所以
- **边际分布精确保留**（配额是整数分配出来的，构造性保证，不靠概率收敛）；
- **α 只由 σ_e 这一个自由度控制** → 一维搜索即可（对目标 α 取 9 个候选 σ_e，
  再在跨零区间线性细化 4 次，总成本约 10~13 次 `O(k·n)` 全量扫描，纯 stdlib）。

## 3. 与本仓库现有模型的接缝

| 现状 | 冲突点 | 处置 |
|---|---|---|
| `answering_v2.generate_answer(question)` **逐题无状态** | α 是**联合**性质，逐题独立采样在数学上只能给出 α≈随机值 | 引入"批次计划矩阵"：先算完 n×k，投递时逐题**查表**，查不到才回退现有加权随机 |
| `WEIGHT_CONFIG` + 题干锚定（`src/anchoring.py`）给的是**边际**约束 | 边际 + α 两个约束不一定同时可达（配额把可选的秩排列限死了） | 明确优先级：**保边际优先**，α 只逼近；不达标就告警并打印实测 α（对标源也是这个取舍，但它没告诉用户） |
| `persona`（v3.2）一份问卷一个人 | 计划矩阵的行索引必须与 persona 轮换同序 | 计划按 `submission_index` 存，Step 0 换人时同时领取该行 |
| `history.answers(run_id, submission_index, question_number, options_selected)` | 实测 α 的**唯一真实数据源** | 已够用：scale/单选存 0-based 索引，等距计分直接用索引+1；矩阵题按行拆需要实现时确认落库形态（现在一题一条，行信息在 `options_selected` 里，P0 先把 matrix 排除在外） |
| 三态判定 success / **unknown** / failed | unknown 的一份**不知道有没有入库**，算进实测 α 会污染 | 实测口径只算 `success`；`unknown` 单列一份"含未知的下界/上界"，别混成一个数 |
| 提交会失败、会中断、会重试 | 计划矩阵的行不能预先占死 | 行只在提交 success 后视为"已消费"；批次中断时未消费的尾行直接丢弃（不做租约超时那套——本工具单实例串行，没有并发争用者） |

## 4. 两个必须显式声明、不能自动猜的东西

1. **维度归属**：哪些题构成同一个构念。自动聚类（按题干相似）会静默地把不相干的题
   算进同一个 α，报出一个漂亮但无意义的数。→ 权重配置 schema 加 `dimension: str`，
   没标的题一律不参与。
2. **反向题**：`±θ` 的符号。靠"题干含负面词"判定，在真卷上错得很难看（本项目的
   探测侧已经吃过关键词误伤的亏）。→ 权重配置加 `reverse: bool`，默认 false；
   未声明的题目一律按正向处理，且当某维度全部正向而实测 α 异常高时提示"是否漏标反向"。

（schema 从 3.0 → 3.1，`src/config_io.py` 要像 v3.0 那次一样做兼容读入 + 报错口径，
不是悄悄加字段。）

## 5. 分期（每期都能单独停下来，且单独有用）

**P0 · 只测不改**（约半天，零行为风险）
- `src/reliability.py`：`cronbach_alpha(rows) -> float` + `by_dimension(run_id)` 从
  `history` 读已提交数据算实测 α。
- CLI `--report-alpha` / GUI 统计区一行：**报当前批次的实测 α**。
- 价值：先看现状 —— 现在这套独立加权随机的 α 到底是多少？如果已经在 0.75+，
  G2 的收益就很小，**方案停在这里最合适**。这是本稿建议先做、且只做这一步的理由。

**P1 · 控制**（约 2~4 天，改答题语义）
- 计划矩阵生成 + 按秩映射 + 一维 σ_e 搜索；`generate_answer` 增加查表入口与回退。
- 配置 schema 3.1（`dimension` / `reverse` / `alpha_target`）。
- GUI：一个"提升问卷信度"开关 + 目标 α 滑条（硬夹 0.60~0.95），旁边**必须**印着
  "不保证实际信度符合预期"——对标源原话如此，这句话是对的。
- 测试：数学层单测（构造 σ_e 生成 2000×8 矩阵，实测 α 落在目标 ±0.02；固定种子可复现；
  边际配额与 `WEIGHT_CONFIG` 逐题完全一致）；离线即可，**不需要**新 E2E —— α 是数据
  结构问题，不是 DOM 问题。

**P2 · 自证**（约 1 天）
- 批次结束打印：计划 α / 实测 α（只算 success）/ 含 unknown 的区间 / 每个维度一行。
- 实测偏离目标超过阈值 → WARN，并说明最可能原因（失败率高、维度题数 <3、反向未声明）。

## 6. 硬约束与降级

- 维度题数 `k < 3` → 不参与（α 无意义，报了就是骗人）。
- 某题 `n < 30` → 该维度只报实测，不做控制（小样本下 σ_e 搜索是在拟合噪声）。
- 搜索后仍不达标 → **保边际**，α 只报告不强行凑。绝不为了 α 去改配额 ——
  配额是用户显式配的，α 不是。
- 计划矩阵与真实题目数量不符（探测题数变化 / 分页导致某页题缺） → 整批退回现有加权
  随机并 WARN 一次，不静默半计划半随机。

## 7. 建议

**先只做 P0。** 它一天不到、不改任何答题行为，而且它给出的数字正好决定 P1 值不值得做
—— 如果当前实现的实测 α 已经偏高，那就没必要引入一整套计划矩阵与两个新配置字段；
如果偏低，那时再拿 P0 的实测数据回来批 P1，判断就有依据了。本仓库一贯的做法也是这个
顺序：先能测，再改。
