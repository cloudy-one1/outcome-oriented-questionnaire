# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environment
venv/
.venv/
# 带后缀的隔离环境（如 .venv310 —— 复现 CI 的 3.10 那条 leg 用）
.venv*/
env/
.env

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Environment & secrets
.env
.env.local
.env.*.local

# Logs
*.log
*.log.*

# Testing
.pytest_cache/
.coverage
coverage.json
htmlcov/

# Jupyter
.ipynb_checkpoints/

# Misc
*.tmp
*.bak

# ---------- 文档：只有沉淀下来的设计稿与评审记录入库 ----------
# docs/ 其余内容仍是本地草稿（一次 git add -A 不该把没写完的稿子带上仓库）。
# 目录级放行写法：`docs/*` 只匹配 docs/ 的一层条目，被 ! 放行的那两个目录
# 里面的文件不再受该模式约束。
docs/*
!docs/design/
!docs/reviews/

# ---------- 运行产物：含 PII，绝不该进版本库 ----------
# data/history.db 的 answers.text_answer 存填空题原文（姓名/手机/邮箱），
# configs/*.json 存用户在权重表里写的候选文本池，导出 CSV 同理。
# 这些路径由工具自身创建（README「运行历史」「GUI 使用」章节），
# 不加进来会让一次 git add -A 直接把敏感数据提交上去。
data/
configs/
*.db
*.sqlite
*.sqlite3
*.csv

# 真实答卷回放的样例表是测试夹具，不是运行产物 —— *.csv 那条是管 data/ 的
!tests/fixtures/replay_answers.csv

# 会话产物，只看本地、不入库（2026-09-23 曾被一次提交带进仓库，同日撤下）。
# 不写根锚定的 `/` 前缀：那两行只管仓库根，把文件挪进任何子目录就会重新可跟踪。
未来优化方向.md
项目评估看板_*.html
